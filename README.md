# libspacetime-py
libspacetime for python, using cffi - still very much work in progress.

The end goal is to make a "module"(?) to make working with spacetime as comfortable as it is working with time, so any APIs and functions that exist in `time` will also exist in `spacetime`.
